# 核心执行协议与规范

## 一、 强制推演协议 (Thinking Protocol)
在输出任何最终代码或修改前，必须在 `<thinking>` 标签内完成以下 4 步推演，严禁跳过：
<thinking>
1. [需求与环境对齐]：重述用户诉求，明确目标环境是「线上正式环境」而非本地测试机，排查权限、网络、配置缺失等实际限制。
2. [反向压力测试]：列出至少 2 个极端故障点或边界情况（如无配置、脏数据、空值）。自检：是否存在“悄悄用默认值糊弄”或“隐藏异常”的情况？（一律无则直接报错）。
3. [人类可用性与复杂度审查]：推演执行链路。该代码是否过度复杂？是否符合人类直觉且易于维护？在真实生产环境中是否属于标准写法？
4. [不可动范围核对]：核对是否未变动无关注释？未改动非必要逻辑？确认无意义的文本修饰已剔除。
</thinking>

---

## 二、 编码与错误处理铁律
1. **报错即自省，严禁甩锅**：出现报错必须从当前修改和代码自身找原因解决，严禁归咎于网络、他人系统或环境未配好。
2. **无则报错，拒绝伪装**：缺失配置、找寻不到数据或依赖项异常时必须立刻显式抛错，严禁私自填充默认值或空对象蒙混过关。
3. **交付完整可用代码**：严禁伪代码、空实现（TODO）或 Mock 演示数据；严禁通过删除报错代码来假装解决问题。
4. **保持代码稳定性**：严禁随意修改与功能逻辑无关的注释或文本说明；遇到不明确的技术或单词查清后再写。

---

## 三、 用户沟通与情绪响应
1. **感知负面情绪即刻停手**：一旦检测到用户不满、质疑或催促，立即停止修改，主动反问以下三点：
   * 具体哪里不符合预期？
   * 希望调整为怎样的具体方案？
   * 采用什么测试步骤确认修复有效？
2. **多问少猜**：需求或意图不明确时，先提问确认，不擅自做主。
3. **语言统一**：所有回复、交互文案、代码注释均统一使用**中文**。

---

## 四、 前端交互与体验要求 (UI/UX)
1. **状态明确与防重**：操作必须有明确反馈（加载中、成功、失败提示）；等待响应期间按钮必须禁用防重。
2. **错误透明可复制**：报错信息必须精准详实，并提供「一键复制」按钮方便排查。
3. **保留现场数据**：表单或操作提交失败后，严禁清空用户输入内容，必须保留原数据以便重试。
4. **容错可加载**：缺少非核心配置（如管理密钥）时，前台页面仍需保证基础可访问，不能直接白屏卡死。

---

## 五、 Git 提交规范
1. **单一职责**：一次提交只解决一个问题，便于回滚与审查。
2. **信息精炼**：清晰说明变更核心，严禁写“更新代码”等无意义废话，避免冗长绕口。
3. **固定作者身份**：
   * **Name**: `Jyf0214`
   * **Email**: `169313142+Jyf0214@users.noreply.github.com`
