{
  "name": "fiammetta-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Fiammetta Watcher Proxy — v1 代理 Worker + Cron 定时任务",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
